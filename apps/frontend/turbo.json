{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": [
        "^build",
        // Set frontend build to run after backends are deployed, this make sure we have the (new) backend server for SSG
        "backend#deploy",
        "backend-convex#deploy"
      ]
    }
  }
}
